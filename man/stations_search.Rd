% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stations.R
\name{stations_search}
\alias{stations_search}
\title{Search for climate stations}
\usage{
stations_search(
  name = NULL,
  climate_id = NULL,
  station_id = NULL,
  prov = NULL,
  coords = NULL,
  dist = NULL,
  interval = NULL,
  normals_years = NULL,
  has_normals = NULL
)
}
\arguments{
\item{name}{Character string. Station name to search for (partial match).}

\item{climate_id}{Character string. Climate identifier (e.g., "101AE00").}

\item{station_id}{Numeric. Station ID.}

\item{prov}{Character string. Province/territory code (e.g., "BC", "ON").}

\item{coords}{Numeric vector of length 2. Coordinates (latitude, longitude).}

\item{dist}{Numeric. Distance in kilometers from coordinates for proximity search.}

\item{interval}{Character. Time interval: "hour", "day", or "month".}

\item{normals_years}{Character. "current" for current normals (1991-2020),
or specific year range (e.g., "1981-2010").}

\item{has_normals}{Logical. If TRUE, only return stations with normals data.}
}
\value{
A tibble of matching climate stations
}
\description{
Search for climate stations by name, coordinates, or province.
}
\examples{
\dontrun{
stations_search("Kamloops")
stations_search(prov = "BC", interval = "hour")
stations_search(coords = c(50.7, -120.3), dist = 20)
}
}
