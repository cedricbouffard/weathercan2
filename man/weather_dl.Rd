% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/weather_dl.R
\name{weather_dl}
\alias{weather_dl}
\title{Download weather data for stations}
\usage{
weather_dl(
  station_ids = NULL,
  climate_ids = NULL,
  station_names = NULL,
  start = NULL,
  end = NULL,
  interval = "day",
  trim = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{station_ids}{Numeric vector. Station ID(s) to download data for.}

\item{climate_ids}{Character vector. Climate identifier(s) (e.g., "101AE00").}

\item{station_names}{Character vector. Station name(s) to search for.}

\item{start}{Character or Date. Start date (e.g., "2018-01-01").}

\item{end}{Character or Date. End date (e.g., "2018-12-31").}

\item{interval}{Character. Time interval: "hour", "day", or "month". Default is "day".}

\item{trim}{Logical. If TRUE, trim to exact date range. Default is FALSE.}

\item{verbose}{Logical. If TRUE, print download progress. Default is TRUE.}
}
\value{
A tibble of weather data
}
\description{
Download historical weather data for one or more stations for a specified date range.
Data can be retrieved at hourly, daily, or monthly intervals.
}
\examples{
\dontrun{
# Download daily data for a station
weather_dl(climate_id = "101AE00", start = "2020-01-01", end = "2020-12-31")

# Download hourly data for multiple stations
weather_dl(station_ids = c(2, 3), start = "2020-06-01", end = "2020-06-30", 
            interval = "hour")

# Download without geometry (faster, no warnings)
weather_dl(climate_id = "101AE00", start = "2020-01-01", end = "2020-12-31"
)
}
}
