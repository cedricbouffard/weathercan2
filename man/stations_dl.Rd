% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stations.R
\name{stations_dl}
\alias{stations_dl}
\title{Download and cache all climate stations}
\usage{
stations_dl(
  refresh = FALSE,
  cache_file = STATIONS_CACHE_FILE,
  cache_dir = NULL,
  max_age = STATIONS_CACHE_MAX_AGE
)
}
\arguments{
\item{refresh}{Logical. If TRUE, re-download the stations from the API.
Default is FALSE, which returns cached data if available.}

\item{cache_file}{Character. Path to cache file. Default is "stations_cache.rds".}

\item{cache_dir}{Character. Directory to save cache file. Default is NULL (current directory).
Use this to share cache across projects (e.g., cache_dir = "data").}

\item{max_age}{Numeric. Maximum age of cache in days before refresh. Default is 7.}
}
\value{
A tibble of climate stations
}
\description{
Downloads all climate stations from the MSC GeoMet API and returns as a tibble.
This function may take some time to complete as it retrieves all available stations.
}
\examples{
\dontrun{
stations_dl()
stations_dl(refresh = TRUE)
stations_dl(cache_dir = "data")
}
}
